import React, {useState} from 'react';
import { StyleSheet, View, Text, Button, Image } from 'react-native';

export default function PhoneForm({ navigation }) {
    return (
        <View>
            <Text>본인인증전화번호 페이지 입니다</Text>
        </View>
    )
}